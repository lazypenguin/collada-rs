/*
use std::fs::{File};

use core::{Asset};

/// Represents an entire collada document in memory
pub struct Collada {
    pub asset: Asset,
}

impl Collada {
    pub fn new() -> Collada {
        Collada {
            asset: Asset::new(),
        }
    }
    
    /// Read a collada document
    pub fn read(file: File) -> Result<Collada, ()>{
        let mut c = Collada::new();
        Ok(c)
    }

}
*/
